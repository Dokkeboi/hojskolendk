---
import { Image } = Astro.props;
---

<div
  class="flex flex-col border w-[500px] items-center py-[52px] px-[33px] place-self-center shadow-[1px_3px_7.199999809265137px_0px] shadow-secondary-charcoal-200"
>
  <form class="flex flex-col gap-5">
    <input
      class="w-96 h-12 rounded-[3px] border-secondary-charcoal-200 font-text text-primary-charcoal"
      type="text"
      placeholder="Fulde navn"
    />
    <input
      class="w-96 h-12 rounded-[3px] border-secondary-charcoal-200 font-text text-primary-charcoal"
      type="text"
      placeholder="E-mail"
    />
    <label class="flex" for="features">
      <select class="w-[85px]" name="feature">
        <option value="">+45</option>
        <option value="">+44</option>
      </select>
      <input
        class="w-[100%] h-12 rounded-r-[3px] border-secondary-charcoal-200 font-text text-primary-charcoal"
        type="text"
        placeholder="Telefon nummer"
      />
    </label>
    <div class="flex flex-col">
      <label for="meeting">Hvilket møde deltager du i?</label>
      <select name="meeting" id="which-meeting"> </select>
    </div>
  </form>
</div>

<style>
  ::placeholder {
    color: var(--color-primary-charcoal);
  }

  input:focus {
    box-shadow: none;
    border-color: var(--color-primary-skypulse);
  }

  select {
    background-image: url("../assets/black-arrow-down.svg");
  }
</style>
