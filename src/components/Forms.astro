---

---

<div class="flex flex-col border w-[500px] items-center">
  <form class="flex flex-col gap-5">
    <input
      class="w-96 h-12 rounded-[3px] border-secondary-charcoal-200 font-text text-primary-charcoal"
      type="text"
      placeholder="Fulde navn"
    />
    <input
      class="w-96 h-12 rounded-[3px] border-secondary-charcoal-200 font-text text-primary-charcoal"
      type="text"
      placeholder="E-mail"
    />
    <label class="flex" for="features">
      <select class="w-[85px]" name="feature">
        <option value="+45"></option>
      </select>
      <input
        class="w-[100%] h-12 rounded-[3px] border-secondary-charcoal-200 font-text text-primary-charcoal"
        type="text"
        placeholder="Telefon nummer"
      />
    </label>
  </form>
</div>

<style>
  ::placeholder {
    color: var(--color-primary-charcoal);
  }

  input:hover {
    border-color: var(--color-primary-suntrail);
  }
  input:focus {
    border-color: var(--color-primary-suntrail);
  }
</style>
