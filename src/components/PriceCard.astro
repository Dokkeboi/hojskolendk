---
const { src, alt } = Astro.props;
const uid = `pc-${Math.random().toString(36).slice(2,9)}`;
---




<div data-uid={uid} class="group relative overflow-hidden flex flex-col max-w-80 md:max-w-lg bg-white border border-gray-300 border-l-4 border-l-primary-suntrail rounded-sm px-3 pt-3 pb-12">


  <!-- Billede container - større højde og object-cover for bedste balance -->
  <div class="relative h-72 overflow-hidden -mx-3 -mt-3 mb-4">
    <img
      src={src}
      alt={alt}
      class="w-full h-full object-cover object-center block transition-all duration-300 ease-out group-hover:opacity-0 group-hover:pointer-events-none"
    />
  </div>
 
  <!-- Tekst container går tilbage til original struktur -->
  <div class="bg-white transform transition-transform duration-300 ease-out group-hover:-translate-y-64">




    <div class="">
      <p class="text-s lg:text-m font-bold text-secondary-charcoal-700 mt-2">17. jan 2026</p>
      <div class="text-container flex flex-row">
        <p class="text-s text-secondary-charcoal-500">Varighed: 3 måneder</p>
        <!-- <p class="text-primary-suntrail font-bold ml-1"> ─SNART UDSOLGT</p> -->
      </div>
      <p class="font-text lg:text-xl font-bold text-primary-charcoal mt-3">Højskole i Maldiverne, Sri Lanka, Bali, Gili Islands, Japan og Australien</p>




      <!-- KATEGORIER: Indhold fader/slider på hover -->
      <div class="">
        <div class="font-text">
          <div class="flex flex-col transform translate-y-2 opacity-0 transition-all duration-200 ease-out group-hover:translate-y-0 group-hover:opacity-100 pointer-events-none group-hover:pointer-events-auto">
            <div class="flex space-x-2 mt-5 shrink-0">
              <button type="button" class="cat-btn px-3 py-1 rounded border text-l" data-text="Kort om kultur, mad og lokale oplevelser.">Kultur</button>
              <button type="button" class="cat-btn px-3 py-1 rounded border text-l" data-text="Praktisk info: Visum, vaccinationer og pakke-tips.">Praktisk</button>
              <button type="button" class="cat-btn px-3 py-1 rounded border text-l" data-text="Prisdetaljer, betalingsplan og hvad der er inkluderet.">Pris</button>
            </div>
            <div class="cat-content font-text text-l text-primary-charcoal mt-3">
              <!-- udfyldes via script -->
            </div>
          </div>
        </div>
      </div>
      <!-- /KATEGORIER -->




    </div>
  </div>








  <div class="absolute left-0 right-0 bottom-0 z-30 bg-white px-3 pt-4 pb-4 border-t border-secondary-charcoal-200 lg:static lg:bg-transparent lg:border-t lg:border-secondary-charcoal-200 lg:px-0 lg:pt-5 lg:pb-0 lg:flex lg:items-center lg:justify-between">
    <h4 class=" text-xl md:text-2xl font-display mb-2 lg:mb-0 lg:mr-">86.000-92.000 DKK</h4>
     <div class="flex space-x-2">
       <button class="bg-primary-skypulse text-white px-4 py-2 rounded text-s font-bold hover:bg-secondary-skypulse-600 transition-colors">
         Se mere
       </button>
       <button class="bg-primary-suntrail text-white px-4 py-2 rounded text-s font-bold hover:bg-secondary-suntrail-700 transition-colors">
         Tilmeld
       </button>
     </div>
   </div>
</div>




<script>
document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('[data-uid]').forEach(card => {
    const btns = Array.from(card.querySelectorAll('.cat-btn'));
    const content = card.querySelector('.cat-content');
    if (!btns.length || !content) return;




    function setActive(i) {
      btns.forEach((b, idx) => {
        b.classList.toggle('bg-primary-skypulse', idx === i);
        b.classList.toggle('text-white', idx === i);
        b.classList.toggle('border-transparent', idx === i);
      });
      content.textContent = (btns[i] as HTMLElement)?.dataset.text || '';
    }




    btns.forEach((b, i) => b.addEventListener('click', () => setActive(i)));
    setActive(0);
  });
});
</script>






Pricecard astro
---
const { src, alt } = Astro.props;
const uid = `pc-${Math.random().toString(36).slice(2,9)}`;
---




<div data-uid={uid} class="group relative overflow-hidden flex flex-col max-w-80 md:max-w-lg bg-white border border-gray-300 border-l-4 border-l-primary-suntrail rounded-sm px-3 pt-3 pb-12">


  <!-- Billede container - større højde og object-cover for bedste balance -->
  <div class="relative h-72 overflow-hidden -mx-3 -mt-3 mb-4">
    <img
      src={src}
      alt={alt}
      class="w-full h-full object-cover object-center block transition-all duration-300 ease-out group-hover:opacity-0 group-hover:pointer-events-none"
    />
  </div>
 
  <!-- Tekst container går tilbage til original struktur -->
  <div class="bg-white transform transition-transform duration-300 ease-out group-hover:-translate-y-64">




    <div class="">
      <p class="text-s lg:text-m font-bold text-secondary-charcoal-700 mt-2">17. jan 2026</p>
      <div class="text-container flex flex-row">
        <p class="text-s text-secondary-charcoal-500">Varighed: 3 måneder</p>
        <!-- <p class="text-primary-suntrail font-bold ml-1"> ─SNART UDSOLGT</p> -->
      </div>
      <p class="font-text lg:text-xl font-bold text-primary-charcoal mt-3">Højskole i Maldiverne, Sri Lanka, Bali, Gili Islands, Japan og Australien</p>




      <!-- KATEGORIER: Indhold fader/slider på hover -->
      <div class="">
        <div class="font-text">
          <div class="flex flex-col transform translate-y-2 opacity-0 transition-all duration-200 ease-out group-hover:translate-y-0 group-hover:opacity-100 pointer-events-none group-hover:pointer-events-auto">
            <div class="flex space-x-2 mt-5 shrink-0">
              <button type="button" class="cat-btn px-3 py-1 rounded border text-l" data-text="Kort om kultur, mad og lokale oplevelser.">Kultur</button>
              <button type="button" class="cat-btn px-3 py-1 rounded border text-l" data-text="Praktisk info: Visum, vaccinationer og pakke-tips.">Praktisk</button>
              <button type="button" class="cat-btn px-3 py-1 rounded border text-l" data-text="Prisdetaljer, betalingsplan og hvad der er inkluderet.">Pris</button>
            </div>
            <div class="cat-content font-text text-l text-primary-charcoal mt-3">
              <!-- udfyldes via script -->
            </div>
          </div>
        </div>
      </div>
      <!-- /KATEGORIER -->




    </div>
  </div>








  <div class="absolute left-0 right-0 bottom-0 z-30 bg-white px-3 pt-4 pb-4 border-t border-secondary-charcoal-200 lg:static lg:bg-transparent lg:border-t lg:border-secondary-charcoal-200 lg:px-0 lg:pt-5 lg:pb-0 lg:flex lg:items-center lg:justify-between">
    <h4 class=" text-xl md:text-2xl font-display mb-2 lg:mb-0 lg:mr-">86.000-92.000 DKK</h4>
     <div class="flex space-x-2">
       <button class="bg-primary-skypulse text-white px-4 py-2 rounded text-s font-bold hover:bg-secondary-skypulse-600 transition-colors">
         Se mere
       </button>
       <button class="bg-primary-suntrail text-white px-4 py-2 rounded text-s font-bold hover:bg-secondary-suntrail-700 transition-colors">
         Tilmeld
       </button>
     </div>
   </div>
</div>




<script>
document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('[data-uid]').forEach(card => {
    const btns = Array.from(card.querySelectorAll('.cat-btn'));
    const content = card.querySelector('.cat-content');
    if (!btns.length || !content) return;




    function setActive(i) {
      btns.forEach((b, idx) => {
        b.classList.toggle('bg-primary-skypulse', idx === i);
        b.classList.toggle('text-white', idx === i);
        b.classList.toggle('border-transparent', idx === i);
      });
      content.textContent = (btns[i] as HTMLElement)?.dataset.text || '';
    }




    btns.forEach((b, i) => b.addEventListener('click', () => setActive(i)));
    setActive(0);
  });
});
</script>



Filterbutton

---
const {buttontext} = Astro.props;
---


<button class="filter-btn text-primary-suntrail border-2 border-primary-suntrail px-4 py-1 rounded text-s font-bold hover:bg-primary-suntrail hover:text-white transition-colors flex items-center gap-2">
  <span class="text-font text-8  button-text">{buttontext}</span>
  <span class="text-font button-x hidden">×</span>
</button>


<script>
document.addEventListener('DOMContentLoaded', () => {
  // Add click handlers to all filter buttons
  document.querySelectorAll('.filter-btn').forEach(button => {
    const buttonX = button.querySelector('.button-x');
   
    button.addEventListener('click', () => {
      const isActive = button.classList.contains('bg-primary-suntrail');
     
      if (isActive) {
        // If button is already active, deactivate it
        button.classList.remove('bg-primary-suntrail', 'text-white');
        button.classList.add('text-primary-suntrail');
        buttonX.classList.add('hidden');
      } else {
        // Add active class to clicked button (same as hover state)
        button.classList.remove('text-primary-suntrail');
        button.classList.add('bg-primary-suntrail', 'text-white');
        buttonX.classList.remove('hidden');
      }
    });
  });
});
</script>


